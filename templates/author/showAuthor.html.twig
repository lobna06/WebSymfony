<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Détails de l'Auteur</title>
   <link rel="stylesheet" href="{{ asset('style.css') }}">
</head>
<body class="author-page">
    <h1>Auteur: {{ author.username }}</h1>
    <img src="{{ asset(author.picture) }}" alt="{{ author.username }}" class="author-image">
    <p>Email: {{ author.email }}</p>
    <p>Nombre de livres: {{ author.nb_books }}</p>
    <p>Bienvenue sur la page de l'auteur!</p>
</body>
</html>
